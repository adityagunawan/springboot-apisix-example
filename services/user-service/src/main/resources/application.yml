spring:
  application:
    name: user-service

server:
  port: ${SERVER_PORT:8081}

security:
  api-token: ${API_TOKEN:dev-secret-token}
  jwt-secret: ${JWT_SECRET:dev-jwt-secret-change-me-please-32-chars}
